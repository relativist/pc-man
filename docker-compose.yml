services:
  pc-man:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_BASE_PATH: "${APP_BASE_PATH:-/}"
    container_name: pc-man
    restart: unless-stopped
    ports:
      - "${APP_PORT:-4173}:80"
